# Configuration SGDI - Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs

# === Base de données ===
DB_HOST=localhost
DB_NAME=sgdi_mvp
DB_USER=root
DB_PASS=

# === Configuration Email/SMTP ===
# Activer l'envoi d'emails (true/false)
EMAIL_ENABLED=true

# Serveur SMTP (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=tls

# Identifiants SMTP
SMTP_USERNAME=votre-email@domaine.com
SMTP_PASSWORD=votre-mot-de-passe-application

# Expéditeur des emails
EMAIL_FROM=noreply@dppg.cm
EMAIL_FROM_NAME=SGDI - MINEE/DPPG

# Email administrateur (pour notifications système)
ADMIN_EMAIL=admin@dppg.cm

# Mode debug (true/false)
EMAIL_DEBUG=false

# === Environnement Railway (détecté automatiquement) ===
# RAILWAY_ENVIRONMENT=production
# Ces variables sont automatiquement définies par Railway
